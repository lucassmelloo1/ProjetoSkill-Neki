package com.example.skillmanagement.dto;

public class LoginResponseDTO {
    private String token;

    public LoginResponseDTO(String token) {
        this.token = token;
    }

    // Outros métodos, como getters e setters
}